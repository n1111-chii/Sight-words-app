# タイ単 — 実装状況(2026-07-27時点)

単一HTMLファイル(`index.html`)+ JSONデータファイル群で構成されるバニラJSアプリ。ビルド不要、`fetch()`でJSONを読み込んで動作する。ローカル確認は `.claude/launch.json`(`_devserver.py`、キャッシュ無効化ヘッダー付きのPython簡易サーバー、port8941)で行う。

## 現在の実装状況

トップ画面は3セクション構成: **単語**(サイトワード)/ **短文** / **Basic**(学校準拠の追加語彙)。加えてホーム画面上部に**単語検索ボックス**がある。

**単語セクションのみ、学校準拠の「25語区切り」構成に再構築済み**(2026-07-27、クロ氏依頼書`kuroko_25word_restructure.md`対応)。ナビゲーションは「100/200/…/1000」→その中の「List1〜4」(各25語)の2階層。「List1〜10」という旧呼称は単語セクションでは廃止(短文セクションは旧来通りList1〜10のまま、Basicはグループ→カテゴリのまま無変更)。

- `fry_words.json`(1000語、List1-10×各100語) — サイトワードのメインデータ。**学校の並び順(実際のFry頻度順)に更新済み**(2026-07-26)。以前はList内アルファベット順だった。
- `basic_words_diff.json`(296語) — `school_words.json`(学校準拠506語+its追加で507語)から`fry_words.json`に無い語だけを抽出した差分データ。8グループ・22カテゴリに分類。
- `list1〜10_sentences.json`(短文データ、合計199文) — 各Listの機能語を使った短文(穴埋め・全文タイピング用)。fry_words.jsonとは独立したデータで、クロスリファレンスの整合性チェックはしていない。
- `fry_phonics.json`(1000語、うちtricky:153語) — フォニックスの「トリッキー判定」(綴りと発音がズレる単語)データ。`phonics_check.py`で生成。

## 完成した機能

### 単語セクション(サイトワード、100/200…1000 → List1-4の25語単位)
- ナビゲーション: 「100」〜「1000」(10個)→選択すると「List1〜4」(各25語)。選択後「この25語を見る」ボタンで単語一覧のみ表示する画面(`screen:'wordlist'`)に遷移可能
- ①読み方(聞く): 基本(自己ペース)/ ゆっくり・はやい(自動テンポ、Space一時停止)。1単位=25語のため通しで再生し終えると結果画面(「もう一度」/「ホームへ」)に遷移
- ②タイピング練習: 基本/音+意味/音のみ/意味のみ の4サブモード + 「はやいモード」トグル(基本・意味・音のみ対象、見本を一瞬表示して消し制限時間内に入力)。25問チェックポイント
- ③ミニテスト(25問)/ ④リトライ(最大5周)/ ⑤まとめテスト(通常/ハイレベル、25問)
- 学習記録(習得済み/練習中/未着手 + 苦手単語)、25語単位・全データのリセット
- トリッキー単語のハイライト表示(読み方・じっくりモード時、タイピング系の不正解フィードバック時)

### 短文セクション
- 穴埋めタイピング(文中の対象語を空欄にして入力)
- 全文タイピング(4段階難易度: 見本あり/音声+和訳/和訳のみ/音声のみ、単語単位の色分け差分判定)
- 進捗は`sentence_progress`で独立管理

### Basicセクション(学校準拠追加語彙)
- 8グループ→22カテゴリの2階層ナビゲーション
- 単語セクションの練習エンジン(①〜⑤)を`source`引数で汎用化して流用(短文のような別実装ではない)
- 進捗(`basic_progress`)・リトライ履歴(`basic_last_unit_test`)とも単語セクションと完全分離

### 横断機能
- 単語検索ボックス(ホーム画面): 英単語/日本語の意味で単語・Basicを横断検索、候補を選ぶとそのList/カテゴリが選択された状態でホームに戻る

### PWA対応(オフライン動作)
- `manifest.json`(`display:standalone`、テーマカラー、アイコン)+ `service-worker.js`(`taitan-cache-v1`)を追加。初回アクセス時にindex.html本体・全JSONデータファイル・アイコンをキャッシュし、以降はオフラインでも起動・操作できる(ローカルサーバーを止めた状態での動作を確認済み)
- キャッシュはバージョン管理(`CACHE_VERSION`)。データファイル更新時はバージョンを上げれば`activate`時に旧キャッシュを自動削除
- アイコン(`icons/icon-192.png`・`icon-512.png`・`apple-touch-icon.png`)はテーマカラー背景+「単」の文字によるプレースホルダー(PowerShellの`System.Drawing`で生成)。後で本番アイコンに差し替え可能
- **iPad Safari実機での「ホーム画面に追加」時の表示・フルスクリーン起動・機内モードでの動作、および実機での音声再生確認は未実施**(自分の環境では検証不可、要ユーザー確認)

## 未完成の機能・既知の課題

- **Basic用のリセットUI**が無い(短文の進捗も同様)。単語セクションの「データのリセット」はList/全データ対応だが、Basic・短文の進捗を消す手段が今のところ無い。
- **「ホーム」ボタンで戻ると短文セクションは常に単語セクション表示に戻る**既知の挙動(`state.source`を持たないため)。Basicは`state.source`経由で正しく元のセクションに戻るよう修正済みだが、短文は意図的に未修正(既存挙動を壊さないための判断)。
- 短文データ(`list1〜10_sentences.json`)は`fry_words.json`の単語構成と紐付いていないため、fry_words.json更新で単語が入れ替わっても短文側は自動追従しない(例: `its`は短文List1に残っているが、サイトワードとしては存在しない。実用上は問題ないと判断し保留)。
- 現在ローカルでコミットが進んでいるが、**GitHubへのpushは未実施**(ユーザーに都度確認して実施する運用)。

## 次にやるべきこと

1. **iPad実機でのPWA動作確認**(ホーム画面追加・フルスクリーン起動・機内モード・音声再生)— ユーザー側での実施が必要
2. プレースホルダーアイコンを本番用デザインに差し替え(要望が出たら)
3. Basic・短文セクションのリセットUI追加を検討(要望が出たら)
4. 短文データとfry_words.jsonの整合性(itsの扱いなど)について、方針が固まれば短文データ側を調整

## 重要な設計判断

- **モード構成**: 各セクションとも①読み方/②タイピング練習/③ミニテスト/④リトライ/⑤まとめテストの5モード構成に統一。Basicセクションは単語セクションの`renderWordModeGrid()`をそのままエイリアス利用(表示が完全にlist非依存な生成のため)。
- **単語データ構造**: `{id, word, list, ja}` (サイトワード) / `{word, ja, category}` + 読み込み時に付与する連番`id` (Basic)。進捗は両方とも**単語文字列をキー**に記録(id/list非依存)。これにより異なるList/カテゴリに同じ綴りの単語が重複しても進捗は共有される(例: Basicの"orange"は色/食べ物2カテゴリに存在するが進捗は1つ)。
- **`source`パラメータによる練習エンジンの汎用化**: `startReading/startSpelling/startUnitTest/startRetest/startReview`は`source`('word'|'basic')を引数に取り、`poolFor(source)`/`progressStoreFor(source)`で参照先を切り替える。デフォルト値は'word'なので既存の単語セクション呼び出しは無変更で動く設計。短文セクションだけはデータ形状が大きく異なる(`{word, sentence, ja, blank_index}`)ため、この汎用化に乗せず**完全に独立したミニ実装**(`startSentenceBlank`/`startSentenceFull`等)にしている。
- **フォニックス表示の適用範囲**: トリッキー単語のハイライト・注記は「読み方・じっくりモード」と「タイピング系の不正解フィードバック」のみに限定。スピードモード(情報を絞る設計思想)と短文セクションには適用していない。
- **全文タイピングの採点**: 表示は単語ごとの部分点的な色分け(仕様上の要求)だが、正誤判定・進捗記録用の内部的な正誤フラグは「単語数が一致し全トークンが正規化後一致」の厳密判定を使用。
- **データ品質チェックの方針**: 短文データ(`blank_index`)や単語データを受け取るたびに、Pythonスクリプトで機械的に整合性検証(空欄位置・重複語チェック)してから反映する運用を徹底(これまでに合計30件以上のズレを発見・修正済み)。
- **単語セクションの25語区切り(2026-07-27)**: `fry_words.json`本体は無改造。既存の`id`(1-1000連番)・`list`(1-10)から`unit = (hundred-1)*4 + quarter`という一意な整数(1-40)を動的算出し、既存の`state.selectedList`にそのまま格納することで、`startReading`等の練習エンジン・`wordsForUnit`ハブに手を入れず新しい25語単位に対応させた。ナビゲーションの二段階選択は、Basicセクションの`basicGroup`+`selectedCategory`という既存パターンを`selectedHundred`+`selectedList`として単語セクションに転用(実装パターンの一貫性を優先)。単位の数え方が変わったことに伴い、直前ミニテスト記録(`swa_last_unit_test`、④リトライ用)のみ初回起動時に一度きり無効化(`swa_unit_scheme`フラグで判定)。学習記録本体(`swa_progress`)・Basic分・短文分には触れていない。検索(⑥辞書検索)は依頼書が想定する「プレーンな文章での辞書表示」ではなく、既存の「検索→クリックでその単位を選択してホームに戻る」フロー(ユーザー確認済みの動作)を維持し、ラベル表記だけ新形式(例:「500 の List1(401番目)」)に更新するに留めた(実装を軽くする判断)。
